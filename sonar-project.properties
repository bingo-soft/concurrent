# Required metadata
sonar.projectKey=Concurrent
sonar.projectName=Concurrent

# Comma-separated paths to directories with sources (required)
sonar.sources=src/

# Language
sonar.language=php

# Encoding of sources files
sonar.sourceEncoding=UTF-8